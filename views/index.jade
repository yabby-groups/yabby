extends layout
block config
  script.
    var config = {
      current: #{current},
      total: #{total},
      limit: #{limit},
      path: '#{path}',
      img_host: '#{img_host}',
      host: '#{host}',
      user: !{JSON.stringify(user)}
    };

block render
  script.
    render_info();
    render_tweets();
