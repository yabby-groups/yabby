extends layout
block config
  script.
    var config = {
      api: '#{api}',
      url: '#{url}',
      img_host: '#{img_host}',
      host: '#{host}',
      user: !{JSON.stringify(user)}
    };

block render
  script.
    render_info();
    render_tweet();

