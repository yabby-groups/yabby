extends layout
block config
  script.
    var config = {
      api: '#{api}',
      url: '#{url}',
      img_host: '#{img_host}',
      host: '#{host}',
      user: !{JSON.stringify(user)},
      tweet: !{JSON.stringify(tweet)}
    };

block render
  script.
    render_info();
    render_tweet();

