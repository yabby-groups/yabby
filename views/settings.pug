extends layout
block config
  script.
    var config = {
      api: '#{api}',
      url: '#{url}',
      img_host: '#{img_host}',
      host: '#{host}',
      user: !{JSON.stringify(user)}
    };

block sidebar
  include sidebar

block render
  script(src="/static/js/jquery.form.js")
  script.
    render_info();
    render_edit_avatar();
    $("#sidebar .menu .settings").addClass('active');
